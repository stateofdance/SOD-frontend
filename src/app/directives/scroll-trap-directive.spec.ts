import { ScrollTrapDirective } from './scroll-trap-directive';

describe('ScrollTrapDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollTrapDirective();
    expect(directive).toBeTruthy();
  });
});
